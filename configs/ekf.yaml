# robot_localization EKF parameters (minimal placeholder)
ekf_filter_node:
  ros__parameters:
    frequency: 30.0
    two_d_mode: true
    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: odom
    # Wire IMU (use angular velocity only for now)
    imu0: /imu/data
    imu0_config: [false, false, false, false, false, false, false, false, false, true, true, true, false, false, false]
    imu0_queue_size: 10
